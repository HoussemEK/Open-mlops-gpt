# Monitoring & drift detection

# MLOps
mlflow>=2.10
zenml[server]>=0.60
sqlalchemy-utils>=0.40.0
evidently>=0.4
dvc[s3]>=3.0

# AWS
boto3>=1.28

# Data
numpy<2.0
pandas>=2.0
scikit-learn>=1.3

--extra-index-url https://download.pytorch.org/whl/cpu

# Core ML (CPU-only to drastically reduce Docker build download size)
torch==2.6.0+cpu
torchvision==0.21.0+cpu
Pillow>=9.5

# Utilities
tqdm>=4.65
sympy==1.13.1
